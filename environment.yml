name: heat
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - pyserial>=3.5
    - fastapi>=0.110.0
    - uvicorn>=0.29.0
    - websockets>=12.0
    - matplotlib>=3.5.0
    - PyYAML>=6.0
    - pydantic>=2.0.0
